result
result-*
.direnv/
.envrc.local
.pre-commit-config.yaml.local
*.swp
.DS_Store

# Design + roadmap docs stay local; docs/reference/ is the public contract
# (JSON schema + man pages) advertised by the lib/CLI and must ship.
docs/*
!docs/reference
!docs/comparison.md

# Local-only future-work tracker
TODO.md
